<%= tr('{user} has mention you in a comment:', user: [@comment.user, :username]) %>

  <%= word_wrap(@comment.plaintext_comment, :line_width => 72).gsub(/\n/, "\n  ") %>

<%= tr('Continue this discussion at {url}', url: @comment.url) %>